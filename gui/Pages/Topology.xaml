<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Per-VM editor. Edit a cell to override the template defaults; Roles are comma-separated."
                   FontSize="13"
                   TextWrapping="Wrap"
                   Margin="8,8,8,8"/>

        <DataGrid x:Name="dgTopology"
                  Grid.Row="1"
                  Margin="8,0,8,0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeaderWidth="0"
                  BorderThickness="0"
                  ColumnHeaderHeight="30">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MahApps.Styles.DataGridColumnHeader}">
                    <Setter Property="Padding" Value="6,0,6,0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell" BasedOn="{StaticResource MahApps.Styles.DataGridCell}">
                    <Setter Property="Padding" Value="6,0,6,0"/>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"        Binding="{Binding Name}"        Width="120"/>
                <DataGridTextColumn Header="Roles"       Binding="{Binding RolesCsv}"    Width="*"/>
                <DataGridTextColumn Header="IP"          Binding="{Binding IP}"          Width="120"/>
                <DataGridTextColumn Header="CPU"         Binding="{Binding Processors}"  Width="60"/>
                <DataGridTextColumn Header="Memory (GB)" Binding="{Binding MemoryGB}"    Width="100"/>
                <DataGridTextColumn Header="Min (GB)"    Binding="{Binding MinMemoryGB}" Width="80"/>
                <DataGridTextColumn Header="Max (GB)"    Binding="{Binding MaxMemoryGB}" Width="80"/>
                <DataGridTextColumn Header="OS Disk (GB)" Binding="{Binding OSDiskGB}"   Width="100"/>
                <DataGridTextColumn Header="Start Delay" Binding="{Binding AutoStartDelay}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="8,12,8,8">
            <Button x:Name="btnTopologyValidate"
                    Style="{DynamicResource MahApps.Styles.Button.Square}"
                    Content="Validate"
                    MinWidth="100"
                    Height="32"
                    Margin="0,0,8,0"
                    Controls:ControlsHelper.ContentCharacterCasing="Normal"/>
            <Button x:Name="btnTopologyBack"
                    Style="{DynamicResource MahApps.Styles.Button.Square}"
                    Content="Back"
                    MinWidth="90"
                    Height="32"
                    Margin="0,0,8,0"
                    Controls:ControlsHelper.ContentCharacterCasing="Normal"/>
            <Button x:Name="btnTopologyNext"
                    Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                    Content="Next: Post-CM"
                    MinWidth="160"
                    Height="32"
                    Controls:ControlsHelper.ContentCharacterCasing="Normal"/>
        </StackPanel>
    </Grid>
</UserControl>
