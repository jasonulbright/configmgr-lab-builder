<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtMediaIntro" Grid.Row="0"
                   Text="The deploy needs these ISOs and installers staged before it starts. Download fetches the direct-link assets into the right folder; Web page opens the vendor download (eval ISOs need a Microsoft sign-in); Folder opens the expected location in Explorer."
                   FontSize="13"
                   TextWrapping="Wrap"
                   Margin="8,8,8,8"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8,4,8,8">
            <Button x:Name="btnMediaRecheck"
                    Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                    Content="Re-check"
                    MinWidth="160"
                    Height="32"
                    Margin="0,0,8,0"
                    Controls:ControlsHelper.ContentCharacterCasing="Normal"/>
            <TextBlock x:Name="txtMediaSummary"
                       Text="Not checked yet."
                       FontSize="12"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource MahApps.Brushes.Gray1}"/>
        </StackPanel>

        <DataGrid x:Name="dgMedia"
                  Grid.Row="2"
                  Margin="8,0,8,0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeaderWidth="0"
                  BorderThickness="0"
                  ColumnHeaderHeight="30">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MahApps.Styles.DataGridColumnHeader}">
                    <Setter Property="Padding" Value="6,0,6,0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell" BasedOn="{StaticResource MahApps.Styles.DataGridCell}">
                    <Setter Property="Padding" Value="6,0,6,0"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Status" Binding="{Binding Glyph}" Width="52"/>
                <DataGridTextColumn Header="Asset" Binding="{Binding Name}" Width="230"/>
                <DataGridTextColumn Header="Detail" Binding="{Binding Detail}" Width="*"/>
                <DataGridTemplateColumn Header="Actions" Width="196" CanUserSort="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="{Binding GetLabel}"
                                        Tag="{Binding GetTag}"
                                        Visibility="{Binding GetVisibility}"
                                        Style="{DynamicResource MahApps.Styles.Button.Square}"
                                        MinWidth="86"
                                        Height="24"
                                        FontSize="11"
                                        Margin="0,2,6,2"
                                        Controls:ControlsHelper.ContentCharacterCasing="Normal"/>
                                <Button Content="Folder"
                                        Tag="{Binding DirTag}"
                                        Style="{DynamicResource MahApps.Styles.Button.Square}"
                                        MinWidth="62"
                                        Height="24"
                                        FontSize="11"
                                        Margin="0,2,0,2"
                                        Controls:ControlsHelper.ContentCharacterCasing="Normal"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="8,12,8,8">
            <Button x:Name="btnMediaBack"
                    Style="{DynamicResource MahApps.Styles.Button.Square}"
                    Content="Back"
                    MinWidth="90"
                    Height="32"
                    Margin="0,0,8,0"
                    Controls:ControlsHelper.ContentCharacterCasing="Normal"/>
            <Button x:Name="btnMediaNext"
                    Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                    Content="Next: Topology"
                    MinWidth="160"
                    Height="32"
                    Controls:ControlsHelper.ContentCharacterCasing="Normal"/>
        </StackPanel>
    </Grid>
</UserControl>
